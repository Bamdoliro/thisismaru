import Button from "@/components/Common/Button/button";

const Home = () => {
  return (
    <div>
      <Button option="PRIMARY" icon={false}>
        더 알아보기
      </Button>
    </div>
  );
};

export default Home;
